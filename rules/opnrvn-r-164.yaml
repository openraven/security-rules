ruleId: opnrvn-r-164
type: asset
ruleName: >
  Checks if Amazon DynamoDB tables are protected by a backup plan.
description: >
  The rule is NON_COMPLIANT if the DynamoDB Table is not covered by a backup plan.
severity: high
enabled: true
sql: >
  select asset_id
  FROM assets
  WHERE resource_type = 'AWS::DynamoDB::Table'
  AND supplementary_configuration->>'awsBackupJobs' = '[]';
remediationDocURLs: https://github.com/openraven/security-rules/wiki
version: 0.9
