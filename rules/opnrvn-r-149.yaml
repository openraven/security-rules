ruleId: opnrvn-r-149
type: asset
ruleName: >
  Ensure Amazon SNS Topic is Encrypted with AWS KMS Key
description: >
  Ensure Amazon SNS Topic is Encrypted with AWS KMS Key. To be compliant an SNS Topic needs to be encrypted with a KMS Key.
severity: high
enabled: true
sql: >
  SELECT asset_id
  FROM assets
  WHERE resource_type = 'AWS::SNS::Topic'
  AND (configuration -> 'attributes' -> 'KmsMasterKeyId' is null);
remediationDocURLs: https://github.com/openraven/security-rules/wiki
version: 0.9
